export enum UserPermissions {
  All = "all",
  RefreshToken = "refresh-token",
  SignOut = "sign-out",

  // ============== users ==========
  GetUsers = "get-users",
  GetUserInfo = "get-user-info",
}